export function closeShippingAssistantWindows(): void {
  // The shipping assistant is rendered inside the main window Taobao workbench.
}

export function registerShippingAssistantHandlers(): void {
  // Legacy no-op: the embedded assistant uses the main preload API directly.
}
